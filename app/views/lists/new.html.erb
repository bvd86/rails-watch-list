<h1 class="list-name">Create a new list</h1>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Create" %>
<% end %>
<p class="cancel"><%= link_to "Cancel", lists_path %></p>
