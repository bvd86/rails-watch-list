<p class="new-list"><%= link_to 'New List', new_list_path(@list) %></p>
<hr>
<div class="list-container">
  <ul>
  <% @lists.each do |list| %>
      <li data-text="<%= list.name %>"><p><%= link_to "#{list.name}", list_path(list) %></p></li>
  <% end %>
  </ul>
</div>
